
{%
  set classes = [
    'region',
    'region-' ~ region|clean_class,
    'hero',
    'is-primary',
  ]
%}
{% if content %}
  <div{{ attributes.addClass(classes) }}>
    <div class="hero-body">
      {{ content }}
    </div>
  </div>
{% endif %}
