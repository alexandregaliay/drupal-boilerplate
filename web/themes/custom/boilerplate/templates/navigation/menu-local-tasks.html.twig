{% if primary %}
  <div class="tabs is-toggle is-fullwitdh">
    <h2 class="visually-hidden">{{ 'Primary tabs'|t }}</h2>
    <ul class="primary">{{ primary }}</ul>
  </div>
{% endif %}
{% if secondary %}
  <div class="tabs is-toggle is-fullwitdh">
    <h2 class="visually-hidden">{{ 'Secondary tabs'|t }}</h2>
    <ul class="secondary">{{ secondary }}</ul>
  </div>
{% endif %}
